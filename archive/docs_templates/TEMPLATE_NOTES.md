# Template Notes

1) Copy folder to a new project name.
2) Run `scripts\bootstrap.ps1 -ProjectName <name> -Domain <domain>`.
3) Run `scripts\validate_template.ps1` to ensure placeholders are replaced.
4) Update `docs/LOGBOOK.md`, `AGENTS.md`, `docs/PERMISSIONS.md`.
